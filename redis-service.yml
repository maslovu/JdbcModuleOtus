apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: ClusterIP # Доступ только внутри кластера
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379 # Порт сервиса
      targetPort: 6379 # Порт контейнера